# Лабораторна робота 1. Робота з СУБД PostgreSQL та основи SQL

## Загальна інформація

**Здобувач освіти:** Зінькевич Олександра Василівна
**Група:** ІПЗ-31
**Обраний рівень складності:** 3

## Виконання завдань

### Список таблиць

```sql
-- Запит для отримання списку таблиць
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
ORDER BY table_name;
```

Результат: У базі даних створено 8 основних таблиць: categories, customers, employees, order_items, orders, products, regions, suppliers.

Скріншот

![screen](screen/table.png)


## Рівень 1

### Отримати всі записи з таблиці customers.

```sql
SELECT * FROM customers;
```

Результат: Отримано 15 записів клієнтів, включаючи як фізичних осіб, так і юридичні особи з різних міст України.

Скріншот

![screen](screen/level1.1.png)
![screen](screen/level1.2.png)


### Вивести назви товарів та їхні ціни

```sql
SELECT product_name, unit_price FROM products;
```

Результат: Отримано перелік усіх товарів із вказанням їхньої вартості

Скріншот

![screen](screen/level1.3.png)
![screen](screen/level1.4.png)
![screen](screen/level1.5.png)


### Показати контактні дані співробітників

```sql
SELECT last_name, first_name, phone, email FROM employees;
```

Результат: Отримано список працівників з їхніми номерами телефонів та електронними поштами.

Скріншот

![screen](screen/level1.6.png)


### Клієнти з міста Київ

```sql
SELECT * FROM customers WHERE city = 'Київ';
```

Результат: Знайдено клієнтів, що зареєстровані в місті Київ.

Скріншот

![screen](screen/level1.7.png)


### Товари дорожчі за 25000 грн

```sql
SELECT * FROM products WHERE unit_price > 25000;
```

Результат: Отримано список товарів з ціною понад 25000 грн.

Скріншот

![screen](screen/level1.8.png)
![screen](screen/level1.9.png)


### Замовлення зі статусом 'delivered'

```sql
SELECT * FROM orders WHERE order_status = 'delivered';
```

Результат: Відфільтровано всі успішно доставлені замовлення.

Скріншот

![screen](screen/level1.10.png)
![screen](screen/level1.11.png)
![screen](screen/level1.12.png)


### Співробітники відділу продажів

```sql
SELECT * FROM employees WHERE title LIKE '%продаж%';
```

Результат: Отримано список співробітників, які працюють у відділі продажів.

Скріншот

![screen](screen/level1.13.png)

### Товари за зростанням ціни

```sql
SELECT * FROM products ORDER BY unit_price ASC;
```

Результат: Товари відсортовано від найдешевшого до найдорожчого.

Скріншот

![screen](screen/level1.14.png)
![screen](screen/level1.15.png)
![screen](screen/level1.16.png)


### Клієнти в алфавітному порядку

```sql
SELECT * FROM customers ORDER BY contact_name ASC;
```

Результат: Перелік клієнтів впорядковано за алфавітом (А-Я).

Скріншот

![screen](screen/level1.17.png)
![screen](screen/level1.18.png)


### Замовлення від найновіших до найстаріших

```sql
SELECT * FROM orders ORDER BY order_date DESC;
```

Результат: Замовлення відсортовано за датою у зворотному хронологічному порядку.

Скріншот

![screen](screen/level1.19.png)
![screen](screen/level1.20.png)
![screen](screen/level1.21.png)


### Перші 10 найдорожчих товарів

```sql
SELECT * FROM products ORDER BY unit_price DESC LIMIT 10;
```

Результат: Отримано топ-10 товарів з найвищою ціною.

Скріншот

![screen](screen/level1.22.png)


### 5 останніх замовлень

```sql
SELECT * FROM orders ORDER BY order_date DESC LIMIT 5;
```

Результат: Отримано 5 найновіших замовлень у системі.

Скріншот

![screen](screen/level1.23.png)


### Перші 8 клієнтів за алфавітом

```sql
SELECT * FROM customers ORDER BY contact_name ASC LIMIT 8;
```

Результат: Виведено перших 8 клієнтів за алфавітним списком.

Скріншот

![screen](screen/level1.24.png)


## Рівень 2

### Пошук клієнтів на ім'я Іван

```sql
-- Імена що починаються на "Іван"
SELECT * FROM customers WHERE contact_name like 'Іван%';
```

Результат: Знайдено всіх клієнтів, ім'я яких починається на 'Іван'.

Скріншот

![screen](screen/level2.1.png)


### Пошук телефонів у назві товарів

```sql
--Назви що містять в собі "phone" або "телефон"
SELECT * FROM products
WHERE product_name ilike '%phone%' or product_name ilike '%телефон%';
```

Результат: Отримано всі товари, які є телефонами або смартфонами.

Скріншот

![screen](screen/level2.2.png)


### Власне: Знайти всіх менеджерів

```sql
-- Знайти всіх менеджерів
SELECT * FROM employees WHERE title LIKE 'Менеджер%';
```

Результат: Отримано співробітників на посаді менеджера.

Скріншот

![screen](screen/level2.3.png)


### Власне: Знайти усі замовлення що відправлялися Новою Поштою або УкрПоштою

```sql
-- Знайти усі замовлення що відправлялися Новою Поштою або УкрПоштою
SELECT * FROM orders WHERE ship_via LIKE '%Пошта';
```

Результат: Знайдено замовлення, відправлені Новою Поштою або УкрПоштою.

Скріншот

![screen](screen/level2.4.png)
![screen](screen/level2.5.png)


### Власне: Містить: товари з "Samsung"

```sql
-- Знайти усю техніку Samsung
SELECT * FROM products WHERE product_name LIKE '%Samsung%';
```

Результат: Виведено товари, назва яких містить "Samsung".

Скріншот

![screen](screen/level2.6.png)


### Товари 15000–50000 грн

```sql
-- 1. Товари 15000–50000 грн
SELECT * FROM products WHERE unit_price > 15000 AND unit_price < 50000;
```

Результат: Отримано товари в ціновому діапазоні 15000–50000 грн.

Скріншот

![screen](screen/level2.7.png)
![screen](screen/level2.8.png)


### Клієнти з Києва або Львова — юридичні особи

```sql
-- 2. Клієнти з Києва або Львова — юридичні особи
SELECT * FROM customers
WHERE (city = 'Київ' OR city = 'Львів') AND customer_type = 'company';
```

Результат: Знайдено компанії з Києва або Львова.

Скріншот

![screen](screen/level2.9.png)


### Власне: Дорогі товари, яких менше 10

```sql
-- 3.1. Дорогі товари, яких менше 10
SELECT * FROM products
WHERE unit_price > 30000 AND units_in_stock < 10;
```

Результат: Виявлено дорогі товари з залишком менше 10 одиниць.

Скріншот

![screen](screen/level2.10.png)


### Власне: Співробітники без email АБО без телефону

```sql
-- 3.2. Співробітники без email АБО без телефону
SELECT * FROM employees WHERE email IS NULL OR phone IS NULL;
```

Результат: Знайдено співробітників без email або телефону.

Скріншот

![screen](screen/level2.11.png)


### Власне: Клієнти НЕ з Києва, які є компаніями

```sql
-- 3.3. Клієнти НЕ з Києва, які є компаніями
SELECT * FROM customers
WHERE NOT city = 'Київ' AND customer_type = 'company';
```

Результат: Отримано компанії не з Києва.

Скріншот

![screen](screen/level2.12.png)


### Власне:  Товари в наявності, але не з категорії 1 та 2

```sql
-- 3.4. Товари в наявності, але не з категорії 1 та 2
SELECT * FROM products
WHERE units_in_stock > 0 AND category_id NOT IN (1, 2);
```

Результат: Виведено товари в наявності, що не належать до категорій 1 та 2.

Скріншот

![screen](screen/level2.13.png)
![screen](screen/level2.14.png)


### Клієнти з Києва, Харкова, Одеси, Дніпра

```sql
-- 1. Клієнти з Києва, Харкова, Одеси, Дніпра
SELECT * FROM customers WHERE city IN ('Київ', 'Харків', 'Одеса', 'Дніпро');
```

Результат: Знайдено клієнтів з чотирьох міст.

Скріншот

![screen](screen/level2.15.png)
![screen](screen/level2.16.png)


### Товари 10000–30000 грн

```sql
-- 2. Товари 10000–30000 грн
SELECT * FROM products WHERE unit_price BETWEEN 10000 AND 30000;
```

Результат: Отримано товари в діапазоні 10000–30000 грн.

Скріншот

![screen](screen/level2.17.png)
![screen](screen/level2.18.png)


### Власне: IN

```sql
-- IN 1 :
SELECT * FROM products WHERE category_id IN (1, 3, 5);  -- кілька категорій
```

Результат: Виведено товари з категорій 1, 3, 5.

Скріншот

![screen](screen/level2.19.png)


```sql
-- IN 2 :
SELECT * FROM orders WHERE order_status IN ('pending', 'processing');  -- активні
```

Результат: Знайдено замовлення зі статусами pending та processing.

Скріншот

![screen](screen/level2.20.png)


### Власне: BETWEEN

```sql
-- BETWEEN 1:
SELECT * FROM orders WHERE order_date BETWEEN '2024-01-01' AND '2024-03-30';
```

Результат: Отримано замовлення за першу чверть 2024 року.

Скріншот

![screen](screen/level2.21.png)


```sql
-- BETWEEN 2:
SELECT * FROM products WHERE units_in_stock BETWEEN 30 AND 100;
```

Результат: Виведено товари із залишком від 30 до 100 одиниць.

Скріншот

![screen](screen/level2.22.png)


### Власне: IS NULL / IS NOT NULL

```sql
-- IS NULL / IS NOT NULL 1 :
SELECT * FROM customers WHERE company_name IS NULL;  -- фізособи
```

Результат: Знайдено фізичних осіб (без назви компанії).

Скріншот

![screen](screen/level2.23.png)


```sql
-- IS NULL / IS NOT NULL 2 :
SELECT * FROM products WHERE description IS NOT NULL;  -- з описом
```

Результат: Отримано товари, що мають опис.

Скріншот

![screen](screen/level2.24.png)
![screen](screen/level2.25.png)
![screen](screen/level2.26.png)


### LIKE + AND + OR

```sql
-- 1. LIKE + AND + OR
-- Мета: Знайти преміум-товари брендів Samsung або Apple,
--       які є в наявності на складі.
-- БІЗНЕС-ЛОГІКА: Відділ маркетингу формує вітрину найдорожчих
--                флагманів від топових брендів для реклами.
--                Дешеві моделі та товари не в наявності
--                не потрапляють у рекламну кампанію.
SELECT * FROM products
WHERE (product_name ILIKE '%samsung%' OR product_name ILIKE '%apple%')
  AND unit_price > 20000
  AND units_in_stock > 0;
```

Результат: Знайдено преміум-товари Samsung або Apple, що є в наявності.

Скріншот

![screen](screen/level2.27.png)


### BETWEEN + IN

```sql
-- 2. BETWEEN + IN
-- Мета: Знайти товари середнього цінового сегмента (5000–30000)
--       у ключових категоріях магазину (1, 2, 3).
-- БІЗНЕС-ЛОГІКА: Відділ закупівель аналізує найбільш
--                конкурентний і масовий ринок — середній
--                ціновий сегмент основних товарних груп.
SELECT * FROM products
WHERE unit_price BETWEEN 5000 AND 30000
  AND category_id IN (1, 2, 3);
```

Результат: Виведено товари середнього цінового сегмента в ключових категоріях.

Скріншот

![screen](screen/level2.28.png)


### IS NOT NULL + LIKE + AND

```sql
-- 3. IS NOT NULL + LIKE + AND
-- Мета: Знайти київських клієнтів з Gmail-адресою та
--       заповненим email.
-- БІЗНЕС-ЛОГІКА: Маркетинг готує email-розсилку для київських
--                клієнтів. Використовуємо тільки Gmail, щоб
--                не потрапити у спам-фільтри корпоративних
--                поштових серверів B2B-клієнтів.
SELECT * FROM customers
WHERE email IS NOT NULL
  AND email LIKE '%@gmail.com'
  AND city = 'Київ';
```

Результат: Знайдено клієнтів з Києва з email на gmail.com.

Скріншот

![screen](screen/level2.29.png)


### NOT LIKE + BETWEEN + OR

```sql
-- 4. NOT LIKE + BETWEEN + OR
-- Мета: Знайти товари для переоцінки/розпродажу:
--       НЕ зарядні кабелі, і при цьому або в ціновому діапазоні
--       10000–50000, або накопичилися на складі (>50 шт.).
-- БІЗНЕС-ЛОГІКА: Керівництво шукає кандидатів на зниження ціни:
--                або середньо-преміум товари, або ті, що
--                залежалися на складі. Зарядні кабелі — дрібний
--                аксесуар, який у цьому аналізі не цікавить.
SELECT * FROM products
WHERE product_name NOT LIKE '%кабель%'
  AND (unit_price BETWEEN 10000 AND 25000 OR units_in_stock > 50);
```

Результат: Виявлено товари для переоцінки/розпродажу (не кабелі).

Скріншот

![screen](screen/level2.30.png)


### IN + IS NULL + AND

```sql
-- 5. IN + IS NULL + AND
-- Мета: Знайти співробітників у Києві або Львові, у яких
--       заповнені І телефон, І email.
-- БІЗНЕС-ЛОГІКА: HR-аудит контактів для внутрішньої
--                комунікації. Потрібні тільки ті співробітники,
--                з ким можна зв'язатися обома каналами —
--                решта йдуть у список на дозаповнення.
SELECT * FROM employees
WHERE city IN ('Київ', 'Львів')
  AND phone IS NOT NULL
  AND email IS NOT NULL;
```

Результат: Отримано співробітників з Києва або Львова з повними контактами.

Скріншот

![screen](screen/level2.31.png)


### Спочатку за категорією, потім ціна за спаданням

```sql
-- 1. Спочатку за категорією, потім ціна за спаданням
SELECT * FROM products
ORDER BY category_id ASC, unit_price DESC;
```

Результат: Товари відсортовано за категорією, потім за спаданням ціни.

Скріншот

![screen](screen/level2.32.png)
![screen](screen/level2.33.png)
![screen](screen/level2.34.png)


### Клієнти: тип, місто, ім'я

```sql
-- 2. Клієнти: тип, місто, ім'я
SELECT * FROM customers
ORDER BY customer_type ASC, city ASC, contact_name ASC;
```

Результат: Клієнти впорядковано за типом, містом та ім'ям.

Скріншот

![screen](screen/level2.35.png)
![screen](screen/level2.36.png)


### Замовлення: статус, дата (новіші спочатку)

```sql
-- 3. Замовлення: статус, дата (новіші спочатку)
SELECT * FROM orders
ORDER BY order_status ASC, order_date DESC;

```

Результат: Замовлення відсортовано за статусом та датою.

Скріншот

![screen](screen/level2.37.png)
![screen](screen/level2.38.png)
![screen](screen/level2.39.png)


### Пагінація: сторінка 1

```sql
-- Запити з OFFSET (пагінація)
-- Сторінка 1 (1-10) 
SELECT * FROM products ORDER BY product_name ASC LIMIT 10 OFFSET 0;
```

Результат: Отримано першу сторінку товарів (1-10).

Скріншот

![screen](screen/level2.40.png)


### Пагінація: сторінка 2

```sql
-- Запити з OFFSET (пагінація)
-- Сторінка 2 (11-20)
SELECT * FROM products ORDER BY product_name ASC LIMIT 10 OFFSET 10;
```

Результат: Отримано другу сторінку товарів (11-20).

Скріншот

![screen](screen/level2.41.png)


## Рівень 3

### Samsung або Apple, але не чохол

```sql
-- Базовий: Samsung або Apple, але не чохол
SELECT * FROM products
WHERE (product_name ILIKE '%samsung%' OR product_name ILIKE '%apple%')
  AND product_name NOT LIKE '%чохол%';
```

Результат: Знайдено товари Samsung або Apple, крім чохлів.

Скріншот

![screen](screen/level3.1.png)


### Власне: Смартфони популярних брендів у середньому ціновому сегменті

```sql
-- 1. Смартфони популярних брендів у середньому ціновому сегменті
SELECT product_name, unit_price FROM products
WHERE (product_name ILIKE '%samsung%' 
    OR product_name ILIKE '%apple%' 
    OR product_name ILIKE '%xiaomi%')
  AND unit_price BETWEEN 8000 AND 30000
  AND NOT discontinued;
```

Результат: Отримано актуальні смартфони Samsung, Apple, Xiaomi у ціновому діапазоні 8000–30000 грн.

Скріншот

![screen](screen/level3.2.png)


### Власне: Клієнти з корпоративними доменами пошти

```sql
-- 2. Клієнти з корпоративними доменами пошти
SELECT contact_name, email FROM customers
WHERE email NOT LIKE '%@outlook%' 
  AND email NOT LIKE '%@meta%'
  AND email NOT LIKE '%@gmail%' 
  AND email NOT LIKE '%@ukr%';
```

Результат: Знайдено клієнтів з корпоративними email.

Скріншот

![screen](screen/level3.3.png)


### Власне: Товари, що НЕ є аксесуарами (виключаємо чохли, кабелі, зарядки)

```sql
-- 3. Товари, що НЕ є аксесуарами (виключаємо чохли, кабелі, зарядки)
SELECT product_name FROM products
WHERE product_name NOT LIKE '%чохол%'
  AND product_name NOT LIKE '%кабель%'
  AND product_name NOT LIKE '%зарядк%';
```

Результат: Виведено товари, що не є аксесуарами

Скріншот

![screen](screen/level3.4.png)
![screen](screen/level3.5.png)
![screen](screen/level3.6.png)


### Власне: Співробітники-менеджери

```sql
-- 4. Співробітники-менеджери 
SELECT first_name, last_name, title FROM employees
WHERE title ILIKE '%менеджер%'
  AND title NOT ILIKE '%спеціаліст%'
  AND title NOT ILIKE '%директор%'
  AND title NOT ILIKE '%бухгалтер%';
```

Результат: Отримано менеджерів без суміжних посад.

Скріншот

![screen](screen/level3.7.png)


### Базовий: дорожче 20000 (кат. 1 або 2) АБО дешевше 5000

```sql
-- Базовий: дорожче 20000 (кат. 1 або 2) АБО дешевше 5000
SELECT * FROM products
WHERE (unit_price > 20000 AND category_id IN (1, 2))
   OR (unit_price < 5000);
```

Результат: Знайдено товари з двох цінових груп

Скріншот

![screen](screen/level3.8.png)
![screen](screen/level3.9.png)


### Власне: VIP-клієнти: (Київ або Львів) І (компанія або з email)

```sql
-- 1. VIP-клієнти: (Київ або Львів) І (компанія або з email)
SELECT * FROM customers
WHERE (city IN ('Київ', 'Львів'))
  AND (customer_type = 'company' OR email IS NOT NULL);
```

Результат: Відібрано VIP-клієнтів.

Скріншот

![screen](screen/level3.10.png)


### Власне: Проблемні товари: (немає на складі І дорогі) АБО (є, але дешеві)

```sql
-- 2. Проблемні товари: (немає на складі І дорогі) АБО (є, але дешеві)
SELECT * FROM products
WHERE (units_in_stock = 0 AND unit_price > 30000)
   OR (units_in_stock > 50 AND unit_price < 5000);
```

Результат: Виявлено проблемні товари.

Скріншот

![screen](screen/level3.11.png)


### Власне: Цільова аудиторія: (молоді клієнти з великих міст) АБО (VIP-компанії)

```sql
-- 3. Цільова аудиторія: (молоді клієнти з великих міст) АБО (VIP-компанії)
SELECT * FROM customers
WHERE (city IN ('Київ', 'Харків', 'Одеса', 'Дніпро') AND customer_type = 'individual')
   OR (customer_type = 'company' AND city = 'Київ');
```

Результат: Отримано цільову аудиторію.

Скріншот

![screen](screen/level3.12.png)


### Звіт товарів з 6 умовами фільтрації

```sql
-- Звіт товарів з 6 умовами фільтрації
-- Мета: Сформувати каталог топових товарів для головної
--       сторінки магазину з додатковим ціновим сегментом.
-- БІЗНЕС-ЛОГІКА: Показуємо тільки активні товари ключових
--                категорій у середньо-преміум сегменті,
--                які є в наявності, мають опис і не є
--                дрібними аксесуарами (чохлами).

SELECT
    product_name      AS "Назва",          -- фільтр 5 (NOT LIKE)
    category_id       AS "Категорія",      -- фільтр 4 (IN)
    unit_price        AS "Ціна",           -- фільтр 1 (BETWEEN)
    units_in_stock    AS "Залишок",        -- фільтр 2 (> 0)
    discontinued      AS "Знято",          -- фільтр 3 (NOT)
    description       AS "Опис",           -- фільтр 6 (IS NOT NULL)
    CASE
        WHEN unit_price < 5000  THEN 'Бюджетний'
        WHEN unit_price < 20000 THEN 'Середній'
        WHEN unit_price < 50000 THEN 'Преміум'
        ELSE 'Люкс'
    END               AS "Сегмент"         -- розрахунковий стовпець
FROM products
WHERE
    unit_price BETWEEN 3000 AND 60000       -- 1. ціновий діапазон
    AND units_in_stock > 0                  -- 2. є в наявності
    AND NOT discontinued                    -- 3. активний
    AND category_id IN (1, 2, 3)            -- 4. ключові категорії
    AND product_name NOT LIKE '%чохол%'     -- 5. не аксесуар
    AND description IS NOT NULL             -- 6. є опис
ORDER BY unit_price DESC
LIMIT 20;
```

Результат: Сформовано каталог топових товарів.

Скріншот

![screen](screen/level3.13.png)


### Аналіз клієнтської бази з множинними критеріями

```sql
-- Аналіз клієнтської бази з множинними критеріями
-- Мета: сегментувати "якісних" клієнтів для CRM-кампанії
SELECT
    contact_name    AS "Клієнт",
    city            AS "Місто",
    customer_type   AS "Тип",
    -- Розрахунковий сегмент (перша TRUE умова перемагає)
    CASE
        WHEN customer_type = 'company' AND city = 'Київ' THEN 'VIP корпоративний'
        WHEN customer_type = 'company' THEN 'Корпоративний'
        WHEN city IN ('Київ', 'Харків', 'Львів') THEN 'Клієнт великого міста'
        ELSE 'Регіональний'
    END             AS "Сегмент"
FROM customers
WHERE
    email IS NOT NULL                           -- є email
    AND phone IS NOT NULL                       -- є телефон
    AND contact_name NOT ILIKE '%тест%'         -- без тестових записів
    AND (                                       -- дужки: AND має вищий пріоритет за OR
        city IN ('Київ', 'Харків', 'Львів', 'Одеса', 'Дніпро')
        OR customer_type = 'company'
    )
ORDER BY
    customer_type DESC,   -- спочатку компанії
    city ASC,             -- потім за містом
    contact_name ASC;     -- потім за ім'ям
```

Результат: Проведено сегментацію клієнтів.

Скріншот

![screen](screen/level3.14.png)
![screen](screen/level3.15.png)


### Розподіл товарів за ціновими сегментами

```sql
-- 1. Розподіл товарів за ціновими сегментами
SELECT
    CASE
        WHEN unit_price < 5000 THEN '1. Бюджетний (<5000)'
        WHEN unit_price < 20000 THEN '2. Середній (5000-20000)'
        WHEN unit_price < 50000 THEN '3. Преміум (20000-50000)'
        ELSE '4. Люкс (50000+)'
    END AS "Сегмент",
    COUNT(*) AS "Кількість",
    ROUND(AVG(unit_price), 2) AS "Середня ціна",
    MIN(unit_price) AS "Мін",
    MAX(unit_price) AS "Макс"
FROM products
GROUP BY 1
ORDER BY 1;
```

Результат: Отримано статистику за сегментами.

Скріншот

![screen](screen/level3.16.png)


###  Товари середнього сегмента з найбільшим запасом

```sql
-- 2. Товари середнього сегмента з найбільшим запасом
SELECT product_name, unit_price, units_in_stock,
       unit_price * units_in_stock AS total_value
FROM products
WHERE unit_price BETWEEN 5000 AND 20000
ORDER BY total_value DESC
LIMIT 10;
```

Результат: Виведено 10 товарів з найбільшою вартістю запасів.

Скріншот

![screen](screen/level3.17.png)


### Частка преміум-товарів у кожній категорії

```sql
-- 3. Частка преміум-товарів у кожній категорії
SELECT
    category_id,
    COUNT(*) FILTER (WHERE unit_price >= 50000) AS premium_count,
    COUNT(*) AS total_count,
    ROUND(100.0 * COUNT(*) FILTER (WHERE unit_price >= 50000) / COUNT(*), 1) AS premium_pct
FROM products
GROUP BY category_id
ORDER BY premium_pct DESC;
```

Результат: Розраховано відсоток преміальних товарів.

Скріншот

![screen](screen/level3.18.png)


### Вартість запасів за ціновими сегментами

```sql
-- 4. Вартість запасів за ціновими сегментами
SELECT
    CASE
        WHEN unit_price < 5000  THEN '1. Бюджетний'
        WHEN unit_price < 20000 THEN '2. Середній'
        WHEN unit_price < 50000 THEN '3. Преміум'
        ELSE '4. Люкс'
    END AS "Сегмент",
    COUNT(*) AS "Позицій",                                   
    SUM(units_in_stock) AS "Всього на складі",               
    ROUND(SUM(unit_price * units_in_stock), 2) AS "Вартість запасу"  
FROM products
WHERE discontinued = false          
GROUP BY 1
ORDER BY 1;
```

Результат: Для кожного цінового сегмента підраховано кількість активних товарних позицій, загальну кількість одиниць на складі та сумарну вартість запасів у гривнях.

Скріншот

![screen](screen/level3.19.png)


### Кількість клієнтів за містами

```sql
-- 1. Кількість клієнтів за містами
SELECT city, COUNT(*) AS clients
FROM customers
GROUP BY city
ORDER BY clients DESC;
```

Результат: Підраховано кількість клієнтів за містами.

Скріншот

![screen](screen/level3.20.png)


### Розподіл за типом у кожному місті

```sql
-- 2. Розподіл за типом у кожному місті
SELECT city, customer_type, COUNT(*) AS cnt
FROM customers
GROUP BY city, customer_type
ORDER BY city, cnt DESC;

```

Результат: Отримано розподіл за типом.

Скріншот

![screen](screen/level3.21.png)


### Міста з переважанням юридичних осіб

```sql
-- 3. Міста з переважанням юридичних осіб
SELECT city,
       COUNT(*) FILTER (WHERE customer_type = 'company') AS companies,
       COUNT(*) FILTER (WHERE customer_type = 'person') AS persons
FROM customers
GROUP BY city
HAVING COUNT(*) FILTER (WHERE customer_type = 'company') 
     > COUNT(*) FILTER (WHERE customer_type = 'person')
ORDER BY companies DESC;
```

Результат: Знайдено міста з переважанням компаній.

Скріншот

![screen](screen/level3.22.png)


### Топ-5 міст за кількістю клієнтів з email

```sql
-- 4. Топ-5 міст за кількістю клієнтів з email
SELECT city, COUNT(*) AS with_email
FROM customers
WHERE email IS NOT NULL
GROUP BY city
ORDER BY with_email DESC
LIMIT 5;
```

Результат: Виведено топ-5 міст.

Скріншот

![screen](screen/level3.23.png)


### Кількість замовлень за місяцями

```sql
-- 1. Кількість замовлень за місяцями
SELECT
    DATE_TRUNC('month', order_date) AS month,
    COUNT(*) AS orders_count
FROM orders
GROUP BY month
ORDER BY month;

```

Результат: Побудовано помісячну динаміку.

Скріншот

![screen](screen/level3.24.png)


### День тижня з найбільшою кількістю замовлень

```sql
-- 2. День тижня з найбільшою кількістю замовлень
SELECT
    TO_CHAR(order_date, 'Day') AS day_of_week,
    COUNT(*) AS orders_count
FROM orders
GROUP BY day_of_week
ORDER BY orders_count DESC;
```

Результат: Визначено найактивніший день тижня.

Скріншот

![screen](screen/level3.25.png)


### Середній час між замовленням і відправкою

```sql
-- 3. Середній час між замовленням і відправкою
SELECT
    AVG(shipped_date - order_date) AS avg_days_to_ship
FROM orders
WHERE shipped_date IS NOT NULL;
```

Результат: Обчислено середній час відправки.

Скріншот

![screen](screen/level3.26.png)


### "На межі": дорогі товари з критично малим залишком

```sql
-- 1. "На межі": дорогі товари з критично малим залишком
SELECT product_name, unit_price, units_in_stock
FROM products
WHERE unit_price > 20000        
  AND units_in_stock BETWEEN 1 AND 10   
ORDER BY unit_price DESC;
```

Результат: Знайдено дорогі товари з малим залишком.

Скріншот

![screen](screen/level3.27.png)


### "Ідеальний клієнт": має email, телефон, компанію, з великого міста

```sql
-- 2. "Ідеальний клієнт": має email, телефон, компанію, з великого міста
SELECT contact_name, city, company_name
FROM customers
WHERE email IS NOT NULL
  AND phone IS NOT NULL
  AND company_name IS NOT NULL
  AND city IN ('Київ', 'Харків', 'Одеса', 'Дніпро', 'Львів')
ORDER BY city, contact_name;
```

Результат: Відібрано ідеальних клієнтів.

Скріншот

![screen](screen/level3.28.png)


### "Без опису": дорогі товари, які не мають опису

```sql
-- 3. "Без опису": дорогі товари, які не мають опису
SELECT product_name, unit_price, units_in_stock
FROM products
WHERE unit_price > 15000            
  AND description IS NULL             
  AND NOT discontinued               
ORDER BY unit_price DESC;
```

Результат: Виявлено дорогі товари без опису

Скріншот

![screen](screen/level3.29.png)


### "Заморожені гроші": дешеві товари з великим запасом

```sql
-- 4. "Заморожені гроші": дешеві товари з великим запасом
SELECT product_name, unit_price, units_in_stock,
       unit_price * units_in_stock AS stock_value
FROM products
WHERE unit_price < 3000            
  AND units_in_stock > 50            
  AND NOT discontinued               
ORDER BY stock_value DESC;
```

Результат: Знайдено дешеві товари з надлишковим запасом.

Скріншот

![screen](screen/level3.30.png)


### "Збалансований кошик": товари, де ціна і залишок обидва вище середнього

```sql
-- 5. "Збалансований кошик": товари, де ціна і залишок обидва вище середнього
SELECT product_name, unit_price, units_in_stock
FROM products
WHERE unit_price > (SELECT AVG(unit_price) FROM products)
  AND units_in_stock > (SELECT AVG(units_in_stock) FROM products)
ORDER BY unit_price DESC;
```

Результат: Отримано збалансовані товари.


Скріншот

![screen](screen/level3.31.png)


## Висновки

**Самооцінка**: [5]

**Обгрунтування**: [У ході лабораторної роботи успішно розгорнуто навчальну базу даних technomart у СУБД PostgreSQL (Supabase) та виконано всі завдання Рівня 3 (вищий рівень). Реалізовано складні аналітичні SQL-запити з використанням LIKE, ILIKE, логічних операторів, вкладених умов, IN, BETWEEN, IS NULL, CASE, агрегатних функцій, FILTER, підзапитів, функцій дати/часу, сортування та пагінації. Для кожного завдання наведено SQL-код, опис результату та підтверджуючий скріншот. Отримано практичні навички аналізу даних, сегментації клієнтів, моніторингу запасів та часових патернів замовлень.]